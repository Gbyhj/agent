# 爆款产品 UX 分析

> 2026-07-28 · 分析 6 个爆款产品，提取可复用的设计模式

---

## 一、ChatGPT — 零摩擦对话

**解决了什么问题**: 自然语言→AI 回答。不需要学习任何语法、不需要配置、不需要编程。

**为什么火**: 
- 界面只有一个输入框。史上最低门槛的 AI 产品。
- "对话"是每个人都会的动作，零学习成本。
- 回答不是搜索链接，是直接的答案。

**UX 亮点**:
```
┌──────────────────────────┐
│  输入框 (永远在底部)       │  ← 用户只需要关注这一个元素
│                          │
│  对话区 (向上滚动)         │  ← 历史自动保存
│                          │
│  侧栏 (对话列表)           │  ← 可以随时切换会话
└──────────────────────────┘
```

**给 Agent 的启示**: 
- ✅ 我们已有 — 单输入框 + 对话历史
- ❌ 缺 — 流式逐字输出（已加） + 对话自动命名 + 搜索历史对话

---

## 二、DeepSeek — 中文原生 + 免费

**解决了什么问题**: 中文用户不需要翻墙、不需要付费就能用顶级 AI。

**为什么火**:
- 完全免费，不限次数
- 中文理解远超 GPT
- 深度思考模式（展示推理过程）
- Web/App 双端

**UX 亮点**:
```
┌──────────────────────────┐
│  深度思考开关              │  ← 用户可以控制"想多深"
│                          │
│  联网搜索开关              │  ← 明确告知"我搜了哪些网页"
│                          │
│  推理过程可视化            │  ← "我来分析一下..." → 透明
└──────────────────────────┘
```

**给 Agent 的启示**:
- ❌ 缺 — 推理过程可视化（用户看到 Agent 在"想什么"）
- ❌ 缺 — 明确的工具调用进度（"正在读取文件...""正在搜索..."）
- ✅ 已有 — 中文优化 + 免费模型路由

---

## 三、Kimi (月之暗面) — 超长上下文

**解决了什么问题**: 一次能"读"一整本书、一个完整项目。200 万 token 上下文窗口。

**为什么火**:
- 传一个 PDF/链接，AI 能读完并回答
- "探索版"能自主上网搜资料
- 适合学生、研究人员、知识工作者

**UX 亮点**:
```
┌──────────────────────────┐
│  文件拖拽上传区            │  ← 拖文件比打字快
│                          │
│  "探索" 按钮               │  ← 一键让 AI 自己去搜资料
│                          │
│  引用标注                  │  ← 每句话标注来源
└──────────────────────────┘
```

**给 Agent 的启示**:
- ❌ 缺 — 拖拽文件直接分析
- ❌ 缺 — 引用来源标注（Agent 说"修改了 agent.py" → 显示具体行）
- ✅ 已有 — 项目上下文注入

---

## 四、Perplexity — AI 搜索

**解决了什么问题**: 搜索不只是给链接，而是直接给答案 + 来源。

**为什么火**:
- 每个回答都有引文标注
- 追问不需要重新搜索
- Pro Search 会自动拆解复杂问题

**UX 亮点**:
```
┌──────────────────────────┐
│  "Pro" 开关               │  ← 简单搜索 vs 深度研究
│                          │
│  回答中的引用编号 [1][2]   │  ← 可信度
│                          │
│  右侧来源面板              │  ← 同时看到答案和来源
│                          │
│  相关追问                 │  ← "你可能还想问..."
└──────────────────────────┘
```

**给 Agent 的启示**:
- ❌ 缺 — 工具调用的来源标注（"这个结论来自 grep 搜索 agent.py:42"）
- ❌ 缺 — 追问建议（Agent 完成一个任务后提示关联任务）
- ✅ 已有 — 免费搜索 + 智能路由

---

## 五、Cursor — AI 编程的"副驾驶"模式

**解决了什么问题**: AI 不是替代程序员，而是坐在旁边帮你写代码的"副驾驶"。

**为什么火**:
- Tab 补全：预测你下一行要写什么
- Cmd+K：选中一段代码，用自然语言修改
- Composer：一个 prompt 写整个功能
- 所有操作都在 IDE 内，不需要切换窗口

**UX 亮点**:
```
┌──────────────────────────┐
│  Tab = 接受建议           │  ← 最低成本的操作
│  Cmd+K = 局部修改         │  ← 精确控制
│  Composer = 全局生成      │  ← 大范围改动
│                          │
│  内联 Diff 预览            │  ← 改了哪里一目了然
│  Accept/Reject 按钮       │  ← 永远保留否决权
└──────────────────────────┘
```

**给 Agent 的启示**:
- ❌ 缺 — 三级操作粒度 (Tab 快速/局部修改/全局任务)
- ❌ 缺 — 变更预览 (Diff Review)
- ✅ 已有 — Plan/Act 双模式
- ✅ 已有 — 对抗审查

---

## 六、豆包 (字节跳动) — 生态化 AI

**解决了什么问题**: AI 不是一个 App，而是嵌入到你已经在用的所有 App 里。

**特点**:
- 独立 App + 抖音内嵌 + 飞书内嵌
- 语音交互（像打电话）
- 角色扮演（不同 AI 人格）
- 拍题答疑（教育场景）

**UX 亮点**:
```
┌──────────────────────────┐
│  多入口: App/小程序/网页   │  ← 用户在哪都能用
│                          │
│  语音输入                  │  ← 打字累？直接说话
│                          │
│  AI 角色选择               │  ← 不同场景不同人格
│                          │
│  场景化入口                │  ← "帮我写作""翻译""写代码"
└──────────────────────────┘
```

**给 Agent 的启示**:
- ❌ 缺 — 场景化快捷入口（"审查代码""分析项目""重构" → 一键）
- ❌ 缺 — AI 人格切换（不同场景不同角色）
- ✅ 已有 — 三端 (桌面/网页/小程序)
- ✅ 已有 — 任务模板

---

## 七大 UX 改进清单

基于以上分析，我们的 Agent 缺以下 7 个 UX 体验:

| # | 改进 | 灵感来源 | 工作量 |
|:--:|------|---------|:--:|
| 1 | **推理过程可视化** | DeepSeek 深度思考 | 30min |
| 2 | **工具调用进度显示** | Cursor Diff Review | 20min |
| 3 | **引用来源标注** | Perplexity 引文 | 30min |
| 4 | **追问建议** | Perplexity 相关追问 | 15min |
| 5 | **拖拽文件上传** | Kimi 文件分析 | 30min |
| 6 | **场景化快捷入口** | 豆包场景选择 | 15min |
| 7 | **三级操作粒度** | Cursor Tab/Cmd+K/Composer | 1h |

---

## 最核心的一个洞察

> **ChatGPT 的成功不是因为"AI 聪明"，而是因为"人人都会用"。**
> 
> 我们的 Agent 功能很强，但 UX 还不够直觉化。
> 最该优先做的: **推理过程透明化** + **工具调用可感知**。
> 
> 用户不是不信任 AI，用户是不信任自己看不见的过程。
